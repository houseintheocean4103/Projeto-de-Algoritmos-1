#include <iostream>
#include <cmath>

using namespace std;

int main(){
    int v[5], i;
    float raiz[5];

    for (i = 0; i < 5; i++)
        cin>>v[i];

    for (i = 0; i < 5; i++){
        raiz[i] = sqrt(v[i]);

        cout<<"Raiz de "<<v[i] <<" é"<<raiz[i]<<endl;
    }
    return 0;    
}
